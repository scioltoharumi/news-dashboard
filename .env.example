# .env.example — Anthropic API Key などローカル用設定のサンプル
# 実値は書かない。`cp .env.example .env` してから `.env` に実値を入れる。
# `.env` は .gitignore で除外される。

# Anthropic API（§H 到達時に設定）
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# 任意: ログレベル（DEBUG / INFO / WARNING / ERROR）
LOG_LEVEL=INFO
